# Building a Semantic Search Tool — Solution Download

**GitHub:** `module-07-applied-ai/solutions/exercises/expanded-search/`

Compare your solution to the reference. Key things to check:

- Does the source filter actually restrict search results to selected files?
- Do expandable previews show truncated text by default and full text when expanded?
- Is the result count accurate?
- Did you use `@st.cache_resource` for the ChromaDB client?

Your document content, styling, and bonus features will differ. The core requirements are: source filtering, result counts, and expandable previews.