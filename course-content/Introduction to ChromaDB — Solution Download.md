# Introduction to ChromaDB — Solution Download

**GitHub:** `module-07-applied-ai/solutions/exercises/knowledge-base/`

Compare your solution to the reference. Key things to check:

- Is the ChromaDB client persistent (`PersistentClient`)?
- Did you use `upsert()` to handle repeated runs without errors?
- Does metadata filtering work (searching within a specific module)?
- Do results include distance scores for interpretation?
- Can you run the script multiple times without errors or duplicate data?

Your document content and metadata categories will differ. The important thing is the workflow: persistent storage, metadata-enriched documents, and filtered queries.